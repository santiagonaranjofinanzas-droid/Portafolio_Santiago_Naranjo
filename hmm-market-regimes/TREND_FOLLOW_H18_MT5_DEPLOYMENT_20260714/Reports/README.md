#Reports

`RUN_PARITY.ps1` writes one JSON report per magic in this directory. A report is
evidence only when it compares a real MQL signal log against the exact M15 export
from the same MT5 feed and incubation interval.

The package self-test does not constitute observed MQL parity.
